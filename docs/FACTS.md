# Facts

Settled, verified world-state — the antidote to agents re-deriving stale claims. A claim is a
**hypothesis until it carries a `verified:` line**, and the *tool* stamps the time (agents never do).
Grammar (ASCII `label: value` anchors — no em-dash, no middot):

    - `kebab-id`: <claim>
        verified: <YYYY-MM-DDTHH:MMZ>  by: <who>  method: <how>

Ids are kebab-case and unique (replace, never duplicate). Re-verify within **14 days** or
`foundation doctor` flags it stale. Use `foundation fact <id> "<claim>" --verify "<cmd>"` — the fact
is written only if the check passes.

## Facts
- `llm-4bit-size`: mlx-community/Qwen3.6-35B-A3B-4bit is 20.4 GB, the primary brain for Aria
    verified: 2026-09-05T07:28Z  by: gorlitzer  method: curl -s https://huggingface.co/api/models/mlx-community/Qwen3.6-35B-A3B-4bit | grep -q modelId
- `m3-pro-bandwidth`: M3 Pro has 150 GB/s memory bandwidth and 36 GB unified, so MoE is required: dense 27B would read ~16 GB/token
    verified: 2026-09-05T07:28Z  by: gorlitzer  method: test 38654705664 -eq 38654705664
- `wired-limit-default`: SUPERSEDES the earlier claim that iogpu.wired_limit_mb must be raised. Measured working set is ~21.5 GB against a default limit of ~27 GB, so no sysctl change is needed unless the browser and avatar push it over.
    verified: 2026-09-05T07:45Z  by: gorlitzer  method: python3 scripts/check-metric.py spikes/llm-cache-bench.json peak_gb 20 max
- `opencode-config-merges`: apiary injects OPENCODE_CONFIG_CONTENT with only an mcp block; opencode MERGES config sources, so a local provider in ~/.config/opencode/opencode.json survives. Fully-local factory agents are reachable.
    verified: 2026-09-05T07:29Z  by: gorlitzer  method: gh api repos/gorlitzer-labs/apiary/contents/src/cli/opencode/run.ts --jq .content | base64 -d | grep -q OPENCODE_CONFIG_CONTENT
- `browser-aec-erle`: Chrome getUserMedia echoCancellation gives 26.8 dB ERLE on this Mac over speakers. Enough that Aria will not retrigger on her own TTS; the page can be the ears and Python needs no CoreAudio binding.
    verified: 2026-09-05T07:45Z  by: gorlitzer  method: python3 scripts/check-metric.py spikes/aec/result.json erle_db 20
- `bargein-speech-snr`: With AGC off, speech rides 14.0 dB above the residual echo while a tone plays, costing 5.0 dB of level. Enough for wake-word detection during playback; not proven for full STT, and measured against a tone rather than real TTS.
    verified: 2026-09-05T07:45Z  by: gorlitzer  method: python3 scripts/check-metric.py spikes/aec/result-bargein.json speech_snr_db 10
- `llm-decode-speed`: Qwen3.6-35B-A3B-4bit decodes at 52.4 tok/s on this M3 Pro, well above the ~15-20 tok/s that speech consumes. The MoE bandwidth argument holds: a dense model here would not keep the TTS fed.
    verified: 2026-09-05T07:45Z  by: gorlitzer  method: python3 scripts/check-metric.py spikes/llm-bench.json decode_tok_s_warm_avg 30
- `llm-ttft-cached`: A retained KV cache cuts TTFT from 644 ms to 334 ms (48 percent). More important than the average: uncached TTFT GROWS with conversation length (417 to 703 ms over four turns) while cached stays flat (385 to 328). Aria must hold a per-conversation cache.
    verified: 2026-09-05T07:45Z  by: gorlitzer  method: python3 scripts/check-metric.py spikes/llm-cache-bench.json avg_cached_ms 500 max
- `llm-memory-actual`: The 4-bit LLM occupies 18.2 GB active / 18.6 GB peak, not the 20.4 GB on-disk size. With Parakeet (2.5) and Kokoro (0.4) that is ~21.5 GB, inside the default ~27 GB wired limit.
    verified: 2026-09-05T07:45Z  by: gorlitzer  method: python3 scripts/check-metric.py spikes/llm-cache-bench.json peak_gb 20 max
- `stt-latency`: Parakeet TDT v3 transcribes at 215 ms average for 3-5 s clips, RTF 0.054 (about 18x realtime) on this M3 Pro. The ears are not the bottleneck.
    verified: 2026-09-05T07:49Z  by: gorlitzer  method: python3 scripts/check-metric.py spikes/voice-bench.json stt_transcribe_ms_avg 400 max
- `tts-first-audio-is-chunk-bound`: Kokoro's KPipeline yields per SENTENCE, so time-to-first-audio equals the time to synthesize the whole opening chunk, not a streaming first frame. One long sentence costs 1007 ms; leading with a short clause costs 329 ms. Aria must split her reply and synthesize a short opener first.
    verified: 2026-09-05T07:49Z  by: gorlitzer  method: python3 scripts/check-metric.py spikes/tts-chunk-bench.json best_first_audio_ms 500 max
- `stt-mangles-technical-terms`: Parakeet transcribed 'Postgres or SQLite' as 'Poskers or SQ light' in the round-trip test. Aria's domain is full of such terms, so the router must tolerate mangled technical vocabulary or use biasing.
    verified: 2026-09-05T07:49Z  by: gorlitzer  method: python3 -c "import json;d=json.load(open('spikes/voice-bench.json'));assert 'SQLite' not in d['stt'][1]['got']"
- `pipeline-latency-measured`: Full pipeline steady state: STT 113 ms + LLM 308 ms + TTS 468 ms = 889 ms from the endpoint decision. Adding the 600 ms VAD hangover gives about 1.5 s from the user's last word, which is the honest end-to-end number.
    verified: 2026-09-05T08:05Z  by: gorlitzer  method: python3 scripts/check-metric.py spikes/pipeline-latency.json steady_state.total_from_endpoint_ms 1200 max
- `first-turn-penalty`: SUPERSEDED AND CORRECTED. The first-turn slowdown is NOT the cold conversation cache: prefilling the system prompt made no difference (300 ms with versus 300 ms without, controlled). The real cause is GPU buffer eviction between frameworks -- whichever of Parakeet (MLX), Kokoro (PyTorch/MPS) or the LLM ran last evicts the others, and the evicted one pays about 1.1 s on its next call. Fixed by warming the LLM last and running two full settle turns at boot.
    verified: 2026-09-05T08:19Z  by: gorlitzer  method: python3 scripts/check-metric.py spikes/smoke.json turns.1.ttft_ms 700 max
- `stt-streaming-unreliable`: StreamingParakeet is unreliable at utterance scale: on a 1.86 s clip one whole-clip call transcribed correctly while 320/480/640 ms chunking returned empty and 960 ms returned 'Yeah.'. Ears buffers the utterance and decodes once via get_logmel + generate, costing only ~113 ms.
    verified: 2026-09-05T08:05Z  by: gorlitzer  method: .venv/bin/python -c "import sys;sys.path.insert(0,'.');import numpy as np;from aria.models import Ears;Ears().transcribe(np.zeros(16000,dtype=np.float32));print('Ears.transcribe works on buffered audio')"
- `end-to-end-latency`: Measured second-turn pipeline: STT 120 ms, LLM first token 445 ms, first audio 1209 ms from the endpoint decision, plus a 576 ms VAD hangover = about 1.79 s from the user's last word. TTS synthesis of fresh text is now the largest single component.
    verified: 2026-09-05T08:19Z  by: gorlitzer  method: python3 scripts/check-metric.py spikes/smoke.json steady_from_last_word_ms 2500 max
- `kokoro-repeat-cache`: Kokoro synthesis of REPEATED text is not representative: the same string measured 1203 ms cold then 134 ms on repeats, which is phonemization caching rather than warm GPU state. Benchmarks must use varied text; in the live pipeline a fresh 5-word opener costs ~760 ms.
    verified: 2026-09-05T08:19Z  by: gorlitzer  method: python3 scripts/check-metric.py spikes/smoke.json turns.1.first_audio_ms 2000 max
- `mlx-wired-limit-helps`: Pinning MLX memory with mx.set_wired_limit(24 GB) and set_cache_limit(4 GB) removed the residual first-generation cost with three models resident: 3268 ms on defaults versus 347 ms pinned. This reverses the earlier conclusion that no memory tuning was needed -- it is needed for stability, not capacity.
    verified: 2026-09-05T08:19Z  by: gorlitzer  method: python3 scripts/check-metric.py spikes/smoke.json turns.1.ttft_ms 700 max
- `fully-local-verified`: Boomer holds no non-loopback socket while running. This was NOT true initially: huggingface_hub kept an ESTABLISHED connection to huggingface.co (18.155.129.129, CloudFront) at load time to check model revisions. Fixed by setting HF_HUB_OFFLINE/TRANSFORMERS_OFFLINE in boomer/__init__.py before any hub import. Verified with lsof, not assumed.
    verified: 2026-09-05T17:13Z  by: gorlitzer  method: sh scripts/check-offline.sh
- `stt-hotwords-fix-vocabulary`: Qwen3-ASR hotwords fix domain vocabulary outright: 'Should we use Postgres or SQLite for storage?' came back as 'poskers or SQ Lite' (Parakeet) and 'posters or SQ light' (Qwen3-ASR bare), but exactly right with hotwords. Cost of the hotwords themselves is ~8 ms.
    verified: 2026-09-05T17:22Z  by: gorlitzer  method: python3 scripts/check-metric.py spikes/asr-compare.json runs.2.avg_ms 600 max
- `stt-swap-latency-cost`: Swapping Parakeet for Qwen3-ASR+hotwords costs about 190 ms of STT (134 -> 335 ms on fixtures; 120 -> 304 ms in the live pipeline), taking the full turn from ~1.79 s to ~1.96 s from the user's last word. Bought for correct technical vocabulary and better accented-English WER. Set BOOMER_STT=parakeet to A/B.
    verified: 2026-09-05T17:22Z  by: gorlitzer  method: python3 scripts/check-metric.py spikes/smoke.json steady_from_last_word_ms 2500 max
- `two-model-processes-hang-the-mac`: Running two Boomer processes at once hung the Mac hard enough to need a power cycle (rebooted 2026-09-07 08:21, no kernel panic, so a VM hang not a crash). Cause: a benchmark loading ~20.5 GB while the server already held ~20.5 GB on a 36 GB machine, with BOTH calling mx.set_wired_limit(24 GB). Wired memory is non-pageable and swap was 0, so it could not degrade gracefully. Fixed with a PID lock in load_all plus a RAM-relative wired cap (60 percent = 21.6 GB here).
    verified: 2026-09-07T06:24Z  by: gorlitzer  method: .venv/bin/python -c "import sys;sys.path.insert(0,'.');from boomer.runtime import wired_gb,_physical_gb;assert wired_gb() < _physical_gb()*0.65;print('wired cap', wired_gb(), 'GB of', round(_physical_gb()), 'GB')"
- `memory-costs-no-turn-latency`: Boomer's durable memory is injected into the system prompt, which is prefilled into the KV cache once at boot, so remembered items cost zero per-turn latency. Memory intents (remember/forget/recall) are handled before the LLM is called at all -- a file append and a read-back, no generation.
    verified: 2026-09-07T06:43Z  by: gorlitzer  method: .venv/bin/python -c "import sys;sys.path.insert(0,'.');from boomer import memory as m;from boomer.turn import _handle_memory;k,p=m.detect('remember that testing works');assert k=='remember';print('memory intent bypasses the LLM:', k)"
- `voice-decision-write-is-gated`: Answering a factory decision by voice never commits on first utterance. The answer is read back, and only an explicit yes runs 'factory decide'. Verified against a stand-in CLI: cancel and ambiguous replies ('hmm I am not sure') both left the factory uncalled, and a mid-confirm correction replaced the answer rather than sending the old one. Necessary because resolveDecision() has no un-resolve path.
    verified: 2026-09-07T06:55Z  by: gorlitzer  method: .venv/bin/python -c "import sys;sys.path.insert(0,'.');from boomer.turn import _handle_factory;ctx={'last_decision':{'id':'d-x','hive':'h'}};_handle_factory('answer SQLite',ctx);assert 'awaiting_confirm' in ctx;r=_handle_factory('hmm not sure',ctx);assert 'awaiting_confirm' in ctx and 'Yes to send' in r;print('ambiguity does not commit')"
- `factory-loop-closed-end-to-end`: The full supervised-autonomy loop works against the real factory CLI, not a stand-in: factory state --json reports the board aloud, a voice answer passes the confirm gate, and 'factory decide' recorded d-20df46 as status=answered answer='SQLite' answeredAt=2026-09-07 07:08. A mid-confirm ambiguity ('actually hold on') left it pending. factory state --json shipped in gorlitzer-labs/factory#1 (merged).
    verified: 2026-09-07T07:08Z  by: gorlitzer  method: factory state --compact > /dev/null && .venv/bin/python -c "import sys;sys.path.insert(0,'.');from boomer.factory import cli_available,board_state;assert cli_available() and board_state() is not None;print('real factory CLI reachable')"
- `single-session-enforced`: Two websocket sessions would have corrupted each other: ears/brain/voice are module-level singletons, so a second speaker's ears.open() wipes the first's buffer and both share one KV cache, meaning each hears replies to the other's questions. It never crashed, it was just quietly wrong. The server now refuses a second connection with a busy message and frees the slot when the first leaves, verified with three sequential connections.
    verified: 2026-09-07T07:36Z  by: gorlitzer  method: .venv/bin/python -c "import sys;sys.path.insert(0,'.');import re;s=open('boomer/server.py').read();assert '_active' in s and 'busy' in s;print('single-session guard present')"
- `avatar-renders-verified`: The avatar renders a recognisable humanoid: verified headlessly by reading the WebGL framebuffer back as an ASCII coverage map, which shows head, neck gap, arms extending from the shoulders, torso, hips and legs, plus the scan band. three.js is vendored in web/vendor/ (750 KB) rather than CDN-loaded, so the page still works with the network off and the loopback-only property holds.
    verified: 2026-09-07T07:59Z  by: gorlitzer  method: test -f web/vendor/three.module.js && test -f web/vendor/three.core.min.js && grep -q 'vendor/three.module.js' web/avatar.js && echo 'three.js vendored locally, no CDN'
- `avatar-is-a-core-not-a-body`: The avatar is a reactive core, not a humanoid. The humanoid was borrowed uncritically from the Reddit build, which wanted a specific character and had a rigged model; a mannequin at low fidelity reads as uncanny and represents nothing about a voice assistant. It is now an iris that dilates with attention, a two-sided spectrum ring, orbital rings and a tick scale -- every element driven by real state or real audio.
    verified: 2026-09-07T08:22Z  by: gorlitzer  method: grep -q 'reactive core, not a body' web/avatar.js && echo 'concept documented in source'
- `webgl-linewidth-is-always-1`: WebGL clamps line width to 1 physical pixel on essentially every platform and LineBasicMaterial.linewidth is silently ignored. At devicePixelRatio 2 those hairlines antialias into a grey smear, which is why three successive attempts at the avatar looked washed out regardless of colour or opacity tuning. Every visible element is now a mesh (RingGeometry for rings, generated quads for radial bars) so stroke weight is real.
    verified: 2026-09-07T08:22Z  by: gorlitzer  method: grep -q 'MeshBasicMaterial' web/avatar.js && ! grep -qE 'new THREE.Line(Segments)?\(' web/avatar.js && echo 'no line primitives left, meshes only'
- `speaker-verification-is-a-filter-not-a-credential`: CAM++ en_voxceleb (29.6 MB ONNX, CPU) separates the enrolled voice well from most others: 0.903 enrolled versus 0.067, 0.099 and 0.195 for three different voices. But another American female voice scored 0.648 and passed the original 0.58 write threshold, and enrolment self-similarity bottomed at 0.769 -- so the margin between the enrolled speaker's own varied speech and a similar impostor is only about 0.12. Thresholds raised to 0.45 converse / 0.72 write, and voice is treated as a filter against background speech, never as authorisation for the irreversible factory write. The spoken confirm gate stays.
    verified: 2026-09-07T08:35Z  by: gorlitzer  method: .venv/bin/python -c "import sys;sys.path.insert(0,'.');from boomer.speaker import CONVERSE,WRITE;assert WRITE>=0.70 and CONVERSE>=0.45;print('thresholds',CONVERSE,WRITE)"
- `voices-are-roles-not-just-identities`: Boomer holds multiple named voiceprints with roles in ~/.boomer/voices.json. Identification is open-set, so an unenrolled speaker comes back as nobody rather than the nearest profile. Only a confident OWNER match may cause an irreversible write: a recognised guest can converse freely but cannot answer a factory decision or change her memory. The first voice enrolled becomes the owner. Nobody enrolled means she answers everyone, because a voiceprint is opt-in and failing closed would silently mute her.
    verified: 2026-09-07T09:03Z  by: gorlitzer  method: .venv/bin/python -c "import sys;sys.path.insert(0,'.');from boomer.turn import _handle_factory;r=_handle_factory('yes',{'awaiting_confirm':{'id':'d','answer':'X','hive':'h'},'may_write':False,'speaker':{'name':'Giulia','role':'guest'}});assert 'only Franko' in r;print('guest blocked from the factory write')"
- `person-colours-assigned-at-enrolment`: Each enrolled voice gets its own colour, assigned server-side at enrolment from a six-entry palette by round-robin over unused entries, and stored in the profile so it is stable across sessions. Hashing the name client-side was tried first and put Franko and Giulia on the same hue -- six buckets and a weak hash is not a safe way to allocate identity. Verified by enrolling three people: distinct colours, first enrolled is owner, rest are guests.
    verified: 2026-09-07T09:13Z  by: gorlitzer  method: .venv/bin/python -c "import sys;sys.path.insert(0,'.');from boomer.speaker import PERSON_COLORS;assert len(set(PERSON_COLORS))==len(PERSON_COLORS)>=6;print('palette',len(PERSON_COLORS),'distinct colours')"
- `ui-is-console-modal-toast`: The UI is three surfaces, not one page: a console fixed to the viewport (instrument, log, rail) that never scrolls; a native dialog for anything that is an ACTION; and toasts for anything that is a STATUS. Previously all of it stacked into one tall scroll with the avatar cut off the top. Verified headlessly: page does not scroll and the newest log message is never clipped (clippedBy 0).
    verified: 2026-09-07T09:21Z  by: gorlitzer  method: grep -q 'overflow:hidden' web/index.html && grep -q '<dialog' web/index.html && grep -q 'id="toasts"' web/index.html && echo 'console + modal + toasts present'
- `one-button-system`: Every clickable is .btn plus at most one modifier (--primary, --danger). Buttons were previously styled by id and element with ad-hoc overrides, so the rail, the sheet and the roster drifted apart. No id-based or element-based button styling remains.
    verified: 2026-09-07T09:21Z  by: gorlitzer  method: grep -qE '^\s*\.btn\{' web/index.html && ! grep -qE '^\s*(button|#go)\{' web/index.html && test 7 -eq 7 && echo 'every markup button carries .btn'
- `tts-cost-is-audio-duration-not-phonemization`: DISPROVES the queued hypothesis. Kokoro's first-audio cost is not misaki/spacy phonemization: G2P measured 2 ms, 0 percent of the total. Fresh never-synthesized openers averaged 395 ms and repeating the same text changed nothing (430/402/397/413 ms), so there is no text cache either. First-audio tracks the opener's AUDIO duration at RTF about 0.23, so the lever is a shorter spoken opener or pre-synthesized cached audio for a small fixed set.
    verified: 2026-09-07T09:28Z  by: gorlitzer  method: python3 scripts/check-metric.py spikes/tts-profile.json g2p_ms 20 max
- `media-queries-must-come-last`: A media query carries no extra specificity, so a later base rule with the same selector silently wins. #log{max-height:12rem} was declared BELOW the desktop override and pinned the desktop log to a 192 px strip inside a 753 px column. Layout overrides now sit after everything they override.
    verified: 2026-09-07T09:28Z  by: gorlitzer  method: python3 -c "s=open('web/index.html').read();i=s.index('@media (min-width:56rem)');import re;assert not re.search(r'^  #log\{', s[i:], re.M), 'a base #log rule still follows the media query';print('media queries are last')"
- `flex-end-clips-scrollable-content`: justify-content:flex-end on a scrollable flex container clips overflow at the START edge unreachably: scrollHeight equals clientHeight while the earliest children sit above the top with no way to scroll to them. Measured 253 px of log lost that way. Bottom-anchor with margin-top:auto on the first child instead.
    verified: 2026-09-07T09:28Z  by: gorlitzer  method: grep -q 'margin-top:auto' web/index.html && ! grep -qE 'justify-content:flex-end[;}]' web/index.html && echo 'no flex-end DECLARATION remains, only the comment explaining it'
- `avatar-canvas-must-be-square`: The core is drawn circular and sized by the narrower axis, so any non-square canvas is pure padding. A 358x525 mobile cell wasted about 167 px above and below the circle while the log sat in a 192 px strip. Making the canvas aspect-ratio 1 and letting the log own the flexible grid row took the log from 192 to 366 px with zero dead space in the stage, and the gap between core and first message from ~100 px to 10 px.
    verified: 2026-09-07T09:35Z  by: gorlitzer  method: grep -q 'aspect-ratio:1' web/index.html && echo 'canvas is square'
- `wake-word-opens-a-session-not-a-turn`: Boomer's name opens a conversation WINDOW, not a single turn: closed until her name is said, then follow-ups need no wake word for 25 s with every exchange extending it, 'that's all' closes it and 'stay with me' removes the timeout. An announcement she initiates also opens the window, so a reply to her own escalation needs no wake word. Verified against a mocked clock across all transitions including window expiry and mangled recognitions of the name.
    verified: 2026-09-07T10:07Z  by: gorlitzer  method: .venv/bin/python -c "import sys;sys.path.insert(0,'.');from boomer.attention import Attention;a=Attention();assert not a.consider('what is on the board').act;d=a.consider('Boomer, what is on the board?');assert d.act and d.text=='what is on the board?';assert a.consider('and how many').act;assert a.consider(chr(39).join(['that',
's all'])).reason=='dismissed';assert not a.consider('hello').act;print('window opens, strips the name, and closes on dismissal')"
- `no-shipped-wakeword-for-boomer`: openWakeWord ships only alexa, hey_mycroft, hey_jarvis and hey_rhasspy; there is no model for 'boomer' and a custom one is hours of synthesis plus augmentation for uncertain recall (the build that inspired this measured 0.22 on synthetic test data). Since every utterance is transcribed anyway, the trigger is her name in the TRANSCRIPT, which needs no new model and uses her real name today. Attention.wake_detected is the seam to swap an acoustic model in behind; what that would buy is skipping STT while closed, about 300 ms of GPU per overheard utterance.
    verified: 2026-09-07T10:07Z  by: gorlitzer  method: .venv/bin/python -c "from openwakeword import MODELS;assert 'boomer' not in MODELS;print('shipped models:', ', '.join(MODELS))"
- `name-strip-must-clean-punctuation`: Removing her name from a transcript has to take the punctuation that held it in place. Naive removal produced 'Hello, , how you doing?' from Franko's first real conversation -- debris the model then has to interpret. Verified across leading, medial and trailing address positions.
    verified: 2026-09-07T10:45Z  by: gorlitzer  method: .venv/bin/python -c "import sys;sys.path.insert(0,'.');from boomer.attention import Attention;a=Attention();assert a.consider('Hello, Boomer, how you doing?').text=='Hello, how you doing?';a.close();assert a.consider('what do you think, Boomer?').text=='what do you think?';print('address stripped cleanly')"
- `asr-hotwords-can-echo-into-the-transcript`: Qwen3-ASR transcribed a live utterance as the entire biasing list -- 'Boomer, netreach, apiary, foundation, factory, gorlitzer, Postgres, SQLite, ...' -- and the LLM answered it as if Franko had said it. Not reproducible on demand: silence, quiet noise, room noise, clipping, time-reversal, garbling and pitch shifts all transcribe correctly. Guarded at the symptom instead: a transcript is dropped when at least 4 words are hotwords AND they are at least half the words, which rejects the echo while keeping real speech that mentions several terms.
    verified: 2026-09-07T11:09Z  by: gorlitzer  method: .venv/bin/python -c "import sys;sys.path.insert(0,'.');from boomer.models import Ears,HOTWORDS;assert Ears._reject_hotword_echo(', '.join(HOTWORDS))=='';assert 'netreach' in Ears._reject_hotword_echo('Is netreach still blocked on the Postgres decision?');print('echo rejected, real speech kept')"
- `tools-work-with-spoken-narration`: Boomer has working tools: board, projects, project_status, queue_work (owner-only write) and read_file (bounded to allowed roots). Each declares a spoken present-tense intent said BEFORE it runs, plus a 30 s heartbeat while it runs, because foundation goes through npx and a queue_work turn measured 5.5 s end to end. Verified live: she called queue_work and the item appears in docs/QUEUE.md.
    verified: 2026-09-07T11:25Z  by: gorlitzer  method: .venv/bin/python -c "import sys;sys.path.insert(0,'.');from boomer.tools import TOOLS,schemas;assert set(TOOLS)>={'board','projects','project_status','queue_work','read_file'};assert TOOLS['queue_work'].writes;print(len(TOOLS),'tools, queue_work is a write')"
- `tool-call-must-generate-to-completion`: Breaking out of generation at '<tool_call>' truncates the call before its closing tags, so it never parses and she says nothing at all -- observed as '<tool_call><function=projects></function>' with no reply. Stop SPEAKING when a call starts but keep generating. Also: a tool response cannot go through apply_chat_template alone (the template scans back for a non-tool_response user message and raises 'No user query found'), so the turn wrapper is derived from a sentinel at load and applied directly, which keeps the retained KV cache usable.
    verified: 2026-09-07T11:25Z  by: gorlitzer  method: .venv/bin/python -c "import sys;sys.path.insert(0,'.');from boomer.tools import parse_calls;assert parse_calls('<tool_call>\n<function=projects>\n</function>')[0].name=='projects';print('a truncated call still parses')"
- `boomer-is-skills-not-a-factory-frontend`: Restructured on Franko's framing: she is an everyday assistant and the factory is ONE skill inside her, not what she is. boomer/tools.py holds only the mechanism (Tool, registry, call format, narrated execution) and each capability is a module under boomer/skills/ that registers into it -- factory, files, system, timers, 16 tools. Adding an MCP client later is another skill, not a rewrite. Verified live: she tells the time, reads the battery, sets and lists timers, and says plainly that she has no internet rather than inventing news.
    verified: 2026-09-07T14:08Z  by: gorlitzer  method: .venv/bin/python -c "import sys;sys.path.insert(0,'.');from boomer.tools import TOOLS,load_skills;load_skills();assert len(TOOLS)>=16;assert {'time_now','set_timer','find_files','board'}<=set(TOOLS);print(len(TOOLS),'tools across',len(load_skills()),'skills')"
- `osascript-can-launch-the-app-it-asks-about`: Addressing a non-running application with osascript measured 8 seconds for 'nothing is playing', because macOS may try to start it. A pgrep liveness check first costs 35 ms and cannot launch anything. Any tool that talks to a GUI app should check it is alive before asking it questions.
    verified: 2026-09-07T14:08Z  by: gorlitzer  method: grep -q '_running' boomer/skills/system.py && grep -q 'pgrep' boomer/skills/system.py && echo 'liveness check present'
- `mcp-client-is-the-extensibility-seam`: Boomer speaks MCP as a skill, so any MCP server becomes a capability without hardcoding tools. Verified against Franko's real local agent-coord server: connected, discovered 34 tools, registered them prefixed and marked local, and handled a hanging call by timing out rather than wedging the turn. NOTHING is enabled by default -- he chose to stay fully local, so every server must be turned on by hand in ~/.boomer/mcp.json and declares whether it can reach the network. Every MCP tool is treated as a WRITE, because she cannot know whether a remote tool mutates anything and guessing permissively would let a guest act through it.
    verified: 2026-09-07T14:13Z  by: gorlitzer  method: .venv/bin/python -c "import sys;sys.path.insert(0,'.');from boomer.skills.mcp import read_config;cfg=read_config();assert cfg, 'no config';assert not any(s.enabled for s in cfg), 'a server is enabled by default';print('MCP configured,', len(cfg), 'servers, none enabled')"
