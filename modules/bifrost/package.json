{
  "name": "@gorlitzer-labs/bifrost-tests",
  "version": "1.7.0",
  "private": true,
  "description": "Test harness for the bifrost shell CLI (the tool itself installs via install.sh).",
  "scripts": { "test": "node --test test/*.test.mjs" }
}
