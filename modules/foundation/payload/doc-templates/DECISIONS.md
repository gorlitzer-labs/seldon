# Decisions (ADRs)

Immutable — supersede, never edit. **Log a decision only if all three hold:** hard to reverse **and**
surprising without context **and** it closed a real trade-off. Otherwise it's noise. Allocate with
`foundation decision "<title>"`.

## Index
